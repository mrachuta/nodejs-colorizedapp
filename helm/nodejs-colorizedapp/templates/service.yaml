apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-colorizedapp.fullname" . }}
  labels:
    {{- include "nodejs-colorizedapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-colorizedapp.selectorLabels" . | nindent 4 }}
